## 已完成

需要将downloads该文件夹提供静态访问

添加一个删除的接口  参数 文件名  删除dowwnloads目录下的文件

添加一个测试连接的接口 返回当前正在执行的任务列表 

测试本地服务 高并发场景下  服务器的表现  任务队列的执行是否完整  performance 


## 待完成

调研 https://editor.swagger.io/ 能不能直接发起本地请求 将openapi.yaml 直接打开形成链接的形式 不再需要拖动


puppeteer 抓流获取到的视频也需要 根据传递类型的不同 来确定是返回视频 还是音频